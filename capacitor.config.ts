import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tahir.tiffin.healthy',
  appName: 'healthy-tiffin',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
